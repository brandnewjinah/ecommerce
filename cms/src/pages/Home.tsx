import React, { FC } from "react";

//import styles and assets
import styled from "styled-components";

interface Props {}

const Home: FC<Props> = () => {
  return <Wrapper>cms home</Wrapper>;
};

const Wrapper = styled.div``;

export default Home;
